name: Deploy Conference Website

on:
  push:
    branches: ["main"]

  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: npm
          cache-dependency-path: conference-website/package-lock.json

      - name: Install dependencies
        working-directory: conference-website
        run: npm ci

      - name: Build website
        working-directory: conference-website
        run: npm run build

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v6

      - name: Upload website files
        uses: actions/upload-pages-artifact@v5
        with:
          path: conference-website/dist

      - name: Deploy website
        id: deployment
        uses: actions/deploy-pages@v5